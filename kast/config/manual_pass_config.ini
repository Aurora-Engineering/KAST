[DEFAULT]
# KasterDefinitions: Where is the CSV containing kaster (data translation) definitions?
KasterDefinitionsPath = user_inputs/pybullet/pybullet_kaster_definitions.csv
# KasterMethodsPath: Where is the Python file containing translation functions?
KasterMethodsPath = user_inputs/pybullet/pybullet_kaster_methods.py
# DataType: What type of parsing is required? (current options: 'csv', 'MANUAL_PASS')
DataType = manual
# DataFile: If selected Parser requires a file source, put a path to it here
DataFile = kast/data/pybullet_data.csv
# LowLevelHeaders: Only required for 'MANUAL' DataType; what are the low level headers?
LowLevelHeaders = pose,pose1,pose2

